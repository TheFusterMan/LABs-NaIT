extends template.pug

block nav
    +createMenu(dictMenu, 'Каталог животных')

block content
    main.container
        h1 Каталог животных

        +createGraph(graphData)
        +createFilter(filterData)
        +createSort(sortData)
        +createTable(tableHeader, tableData)